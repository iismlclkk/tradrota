
import React from "react";
import ReactDOM from "react-dom/client";
import TradRotaForm from "./TradRotaForm";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <TradRotaForm />
  </React.StrictMode>
);
